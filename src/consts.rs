/// The default branch to pull from if no branch is supplied.
pub static DEFAULT_BRANCH: &'static str = "master";

/// The default remote to pull from is no remote is specified.
pub static DEFAULT_REMOTE: &'static str = "origin";

/// The deafult username to user if accessing a git repository via ssh.
pub static DEFAULT_SSH_USERNAME: &'static str = "git";
